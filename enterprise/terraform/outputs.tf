# Root outputs. Populated as modules land (see PLAN.md §6). Kept as documented
# placeholders so the eventual operator-facing contract is visible from Phase 0.

# output "app_url" {
#   description = "CloudFront URL serving the Wairz SPA."
#   value       = module.frontend.cloudfront_domain
# }

# output "alb_dns_name" {
#   description = "ALB DNS name (backend origin)."
#   value       = module.backend.alb_dns_name
# }

# output "batch_job_queue" {
#   description = "AWS Batch job queue for Ghidra jobs."
#   value       = module.batch.job_queue_arn
# }
